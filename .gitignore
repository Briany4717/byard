/target
/dist
Cargo.lock
/support/
# Per-project build/cache dir (RFC-0006 §"`.byard/`"): dev caches and the
# `byard build` AOT vector-atlas artifact (`.byard/generated/`) are generated,
# never committed.
.byard/
**/*.rs.bk
*.pdb
.env
.DS_Store
.idea
.vscode
.codegraph
.antigravitycli
# byard-material lives here only temporarily, it graduates to its own
# repository (github.com/byard-framework/byard-material); see RFC-0008.
/byard-material/

# Node / editor extension build artifacts
node_modules/
editors/vscode/dist/
# Gradle (JetBrains plugin) caches & build output
.gradle/
editors/jetbrains/build/